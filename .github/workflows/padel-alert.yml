name: "[DESACTIVE] MVP Angers — remplace par scrape_france.yml"

on:
  workflow_dispatch:

jobs:
  check-tournois:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ce workflow est desactive. Utiliser scrape_france.yml."
